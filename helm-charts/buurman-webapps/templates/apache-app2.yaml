{{- if .Values.apacheApp2.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.apacheApp2.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.apacheApp2.name }}
    release: {{ .Release.Name }}
spec:
  replicas: {{ .Values.apacheApp2.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.apacheApp2.name }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Values.apacheApp2.name }}
        release: {{ .Release.Name }}
    spec:
      containers:
      - name: apache
        image: "{{ .Values.apacheApp2.image.repository }}:{{ .Values.apacheApp2.image.tag }}"
        imagePullPolicy: {{ .Values.apacheApp2.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.apacheApp2.service.port }}
        volumeMounts:
        - name: html
          mountPath: /usr/local/apache2/htdocs/
        resources:
          {{- toYaml .Values.apacheApp2.resources | nindent 10 }}
      volumes:
      - name: html
        configMap:
          name: {{ .Values.apacheApp2.name }}-html
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.apacheApp2.name }}-html
  namespace: {{ .Values.namespace }}
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
      <title>{{ .Values.apacheApp2.content.title }}</title>
      <style>
        body {
          font-family: Arial;
          padding: 40px;
          background: {{ .Values.apacheApp2.content.backgroundColor }};
        }
      </style>
    </head>
    <body>
      <h1>🏢 Buurman en Buurman IT</h1>
      <h2>{{ .Values.apacheApp2.content.heading }}</h2>
      <p>{{ .Values.apacheApp2.content.description }}</p>
      <p><strong>Environment:</strong> {{ .Values.environment }}</p>
      <p><strong>Release:</strong> {{ .Release.Name }}</p>
    </body>
    </html>
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apacheApp2.name }}-service
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.apacheApp2.service.type }}
  selector:
    app: {{ .Values.apacheApp2.name }}
    release: {{ .Release.Name }}
  ports:
  - port: {{ .Values.apacheApp2.service.port }}
    targetPort: {{ .Values.apacheApp2.service.port }}
{{- end }}
